[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "self-supervised-learning"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "torch",
    "numpy",
    "pyyaml",
    "scikit-learn",
    "matplotlib",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]